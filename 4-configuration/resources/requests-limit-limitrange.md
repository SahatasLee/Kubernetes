เยี่ยมมากครับ 🔥
หัวข้อนี้ “**Resource Request, Limit และ LimitRange**” คือหนึ่งใน **พื้นฐานสำคัญของ Kubernetes scheduling และ resource management**
เพราะมันควบคุมว่า **Pod จะใช้ CPU และ Memory ได้เท่าไร** และ **Scheduler จะตัดสินใจยังไง**

เรามาอธิบายให้เข้าใจทั้งเชิงเทคนิคและภาพรวมง่าย ๆ เลยครับ 👇

---

## ⚙️ **1. Resource Requests และ Limits คืออะไร**

ใน Kubernetes ทุก **Container** ภายใน Pod สามารถกำหนดได้ 2 ค่า:

| ชื่อ           | หน้าที่                                                   |
| :------------- | :-------------------------------------------------------- |
| **`requests`** | บอก *ปริมาณทรัพยากรขั้นต่ำที่ต้องการ* เพื่อให้ Pod รันได้ |
| **`limits`**   | บอก *ปริมาณทรัพยากรสูงสุดที่อนุญาตให้ใช้*                 |

ใช้กับ CPU และ Memory เช่นเดียวกัน (และบาง plugin อาจมี GPU, storage I/O เป็นต้น)

---

## 💡 **2. หน่วยวัด**

| ประเภท     | หน่วย                       | ตัวอย่าง          |
| :--------- | :-------------------------- | :---------------- |
| **CPU**    | 1 core = `1000m` (milliCPU) | `500m` = 0.5 core |
| **Memory** | Bytes, Ki, Mi, Gi           | `256Mi`, `1Gi`    |

---

## 🧩 **3. ตัวอย่าง Pod พร้อม Request/Limit**

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: resource-demo
spec:
  containers:
  - name: app
    image: nginx
    resources:
      requests:
        cpu: "250m"     # ต้องการขั้นต่ำ 0.25 core
        memory: "128Mi" # ต้องการขั้นต่ำ 128Mi
      limits:
        cpu: "500m"     # ใช้ได้สูงสุด 0.5 core
        memory: "256Mi" # ใช้ได้สูงสุด 256Mi
```

---

### 📘 อธิบายการทำงาน:

| ช่วงการทำงาน             | ผลลัพธ์                               |
| :----------------------- | :------------------------------------ |
| CPU ใช้น้อยกว่า 250m     | ปกติ ✅                                |
| CPU ใช้มากถึง 500m       | ปกติ ✅                                |
| CPU ใช้เกิน 500m         | Container ถูก throttle (ลดความเร็ว) ⏬ |
| Memory ใช้น้อยกว่า 256Mi | ปกติ ✅                                |
| Memory ใช้เกิน 256Mi     | Container ถูก kill ❌ (OOMKilled)      |

---

## ⚙️ **4. Requests มีผลต่อ Scheduler**

เมื่อ Scheduler จะวาง Pod → มันจะดูว่า Node ไหนมี “ทรัพยากรเหลือ >= requests”

ตัวอย่าง:

* Node A มี 2 CPU, 4Gi
* Pod ขอ `requests.cpu=1` → ลงได้ถ้า Node ยังเหลือ ≥ 1 CPU

📌 หมายเหตุ: Kubernetes ใช้ requests เป็นตัวคำนวณ scheduling เท่านั้น
ส่วน limit ใช้ตอน runtime เพื่อจำกัดจริง

---

## 🧠 **5. Requests vs Limits สรุปสั้น**

| เปรียบเทียบ      | **Requests**                        | **Limits**                 |
| :--------------- | :---------------------------------- | :------------------------- |
| หน้าที่          | ใช้ในการ “จอง” ทรัพยากร             | ใช้ในการ “จำกัด” การใช้งาน |
| ผลต่อ scheduling | มีผล                                | ไม่มีผล                    |
| ถ้าเกินค่า       | ไม่มีปัญหาทันที (แต่อาจโดน QoS ต่ำ) | CPU throttle / Memory kill |
| ตัวอย่าง         | `cpu: 100m`                         | `cpu: 500m`                |

---

## 🧮 **6. QoS Class (Quality of Service)**

Kubernetes จัดระดับคุณภาพของ Pod ตามการตั้งค่า request/limit เป็น 3 แบบ:

| Class          | เงื่อนไข                         | ลักษณะ                                  |
| :------------- | :------------------------------- | :-------------------------------------- |
| **Guaranteed** | ทุก container มี request = limit | มี priority สูงสุด                      |
| **Burstable**  | มี request < limit               | ปานกลาง                                 |
| **BestEffort** | ไม่กำหนด request/limit เลย       | ล่างสุด (โดน kill ก่อนสุดถ้า Node เต็ม) |

---

## 🧩 **7. LimitRange คืออะไร**

`LimitRange` ใช้กำหนด “ค่ากลาง” ของ resource ใน **Namespace**
คือการบังคับว่า container ใด ๆ ที่สร้างใน namespace นั้นต้องมี request/limit อยู่ในช่วงที่กำหนด

---

### 📄 ตัวอย่าง LimitRange

```yaml
apiVersion: v1
kind: LimitRange
metadata:
  name: limit-range-example
  namespace: dev
spec:
  limits:
  - default:
      cpu: "500m"
      memory: "256Mi"
    defaultRequest:
      cpu: "200m"
      memory: "128Mi"
    type: Container
```

📌 ความหมาย:

* ถ้า Pod ไม่ได้กำหนด request/limit → จะได้ค่าดีฟอลต์นี้อัตโนมัติ
* ถ้าใส่เกินจากที่ Namespace อนุญาต → ถูก reject

---

## 🧭 **8. สรุปภาพรวมการทำงาน**

```
                 ┌────────────────────────────┐
                 │         Scheduler          │
                 └────────────────────────────┘
                              │
                 ดูจาก "requests" ว่า Node ไหนมีทรัพยากรพอ
                              ▼
                 ┌────────────────────────────┐
                 │          Node A            │
                 └────────────────────────────┘
                              │
                 รันจริง → จำกัด "limits" ระหว่างใช้งาน
                              ▼
     ถ้าใช้เกิน limit.cpu → Throttle
     ถ้าใช้เกิน limit.memory → OOMKilled
```

---

## 🧩 **9. ตัวอย่างรวม (Pod + LimitRange)**

```yaml
# LimitRange (apply ที่ namespace)
apiVersion: v1
kind: LimitRange
metadata:
  name: limit-range
  namespace: prod
spec:
  limits:
  - default:
      cpu: "1"
      memory: "512Mi"
    defaultRequest:
      cpu: "500m"
      memory: "256Mi"
    type: Container
---
# Pod (ไม่ระบุ resource)
apiVersion: v1
kind: Pod
metadata:
  name: simple-pod
  namespace: prod
spec:
  containers:
  - name: nginx
    image: nginx
```

➡️ Pod จะได้รับค่า auto จาก LimitRange:

```
requests.cpu = 500m
limits.cpu = 1
requests.memory = 256Mi
limits.memory = 512Mi
```

---

## 📘 **สรุปทั้งหมด**

| แนวคิด         | หน้าที่                | ใช้ที่ไหน | ตัวอย่าง             |
| :------------- | :--------------------- | :-------- | :------------------- |
| **Request**    | จอง resource ขั้นต่ำ   | Container | `cpu: 200m`          |
| **Limit**      | จำกัดสูงสุด            | Container | `cpu: 500m`          |
| **LimitRange** | ตั้ง default และขอบเขต | Namespace | CPU ≤ 1, MEM ≤ 512Mi |

---

อยากไหมครับให้ผมวาด **diagram แสดงการทำงานของ requests/limits ตอน Scheduler และตอน Runtime**
จะเห็นภาพว่ามันจองทรัพยากรยังไง และ throttle ตอนไหน (เข้าใจสุด ๆ เหมือนเห็นใน cluster จริงเลยครับ)
